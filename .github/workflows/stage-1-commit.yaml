name: "Commit stage"

on:
  workflow_call:
    inputs:
      build_datetime:
        description: "Build datetime, set by the CI/CD pipeline workflow"
        required: true
        type: string
      build_timestamp:
        description: "Build timestamp, set by the CI/CD pipeline workflow"
        required: true
        type: string
      build_epoch:
        description: "Build epoch, set by the CI/CD pipeline workflow"
        required: true
        type: string
      nodejs_version:
        description: "Node.js version, set by the CI/CD pipeline workflow"
        required: true
        type: string
      python_version:
        description: "Python version, set by the CI/CD pipeline workflow"
        required: true
        type: string
      terraform_version:
        description: "Terraform version, set by the CI/CD pipeline workflow"
        required: true
        type: string
      version:
        description: "Version of the software, set by the CI/CD pipeline workflow"
        required: true
        type: string

jobs:
  # Removed unnecessary checks for pytest plugin repository:
  # - scan-secrets: Not applicable for a Python package
  # - check-file-format: Not needed
  # - check-markdown-format: Not needed
  # - check-english-usage: Not needed
  # - lint-terraform: Not applicable for a Python package
  # - count-lines-of-code: Not needed
  # - scan-dependencies: Not applicable without proper AWS credentials
  
  placeholder:
    name: "Commit stage placeholder"
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: "Placeholder step"
        run: echo "Commit stage checks removed - add Python-specific linting here if needed"
